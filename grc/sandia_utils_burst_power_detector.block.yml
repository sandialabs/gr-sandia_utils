# auto-generated by grc.converter

id: sandia_utils_burst_power_detector
label: Burst Power Detector
category: '[Sandia]/Sandia Utilities'

parameters:
-   id: naverage
    label: N-Average
    dtype: int
    default: '150'
-   id: nguard
    label: N-Guard Bins
    dtype: int
    default: '10'
-   id: threshold
    label: Threshold (dB)
    dtype: float
    default: '10'
-   id: holdoff
    label: Holdoff (samples)
    dtype: int
    default: '1000'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
-   label: pow
    domain: stream
    dtype: float
asserts:
- ${ naverage > 0 }

templates:
    imports: import sandia_utils
    make: sandia_utils.burst_power_detector(${naverage}, ${nguard}, ${threshold},
        ${holdoff})

file_format: 1
