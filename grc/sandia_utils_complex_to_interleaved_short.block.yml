# auto-generated by grc.converter

id: sandia_utils_complex_to_interleaved_short
label: Complex to IShort
category: '[Sandia]/Sandia Utilities/Type Converters'

parameters:
-   id: vector_input
    label: Vector Input
    dtype: enum
    default: 'False'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
    option_attributes:
        vlen: ['1', '2']
    hide: ${ ('part' if int(vlen) == 1 else 'none') }
-   id: scale
    label: Scale
    dtype: float
    default: '32767'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: short
    vlen: ${ vector_input.vlen }

templates:
    imports: import sandia_utils
    make: sandia_utils.complex_to_interleaved_short(${vector_input}, ${scale})
    callbacks:
    - set_scale(${scale})

documentation: |-
    This block is a clone of the gnuradio complex_to_ishort, but using a Volk kernel with the scaling parameter enabled. Eventually
      this should live in gr_blocks instead of here.

file_format: 1
