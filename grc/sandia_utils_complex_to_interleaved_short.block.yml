# auto-generated by grc.converter

id: sandia_utils_complex_to_interleaved_short
label: Complex to IShort
category: '[Sandia]/Sandia Utilities/Type Converters'

parameters:
-   id: vector_output
    label: Vector Output
    dtype: enum
    default: 'False'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
    option_attributes:
        vlen: [1, 2]
    hide: ${ 'part' if vector_output.vlen == 1 else 'none' }
-   id: scale
    label: Scale
    dtype: float
    default: '32767'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: short
    vlen: ${ vector_output.vlen }

templates:
    imports: import sandia_utils
    make: sandia_utils.complex_to_interleaved_short(${vector_output}, ${scale})
    callbacks:
    - set_scale(${scale})

file_format: 1
