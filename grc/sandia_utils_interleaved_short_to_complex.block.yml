# auto-generated by grc.converter

id: sandia_utils_interleaved_short_to_complex
label: IShort To Complex
category: '[Sandia]/Sandia Utilities/Type Converters'

parameters:
-   id: vector_input
    label: Vector Input
    dtype: enum
    default: 'False'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
    option_attributes:
        vlen: [1, 2]
    hide: ${ 'part' if vector_input.vlen == 1 else 'none' }
-   id: swap
    label: Swap
    dtype: enum
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
    hide: part
-   id: scale
    label: Scale
    dtype: float
    default: '32767'

inputs:
-   domain: stream
    dtype: short
    vlen: ${ vector_input.vlen }

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import sandia_utils
    make: sandia_utils.interleaved_short_to_complex(${vector_input}, ${swap}, ${scale})
    callbacks:
    - set_swap(${swap})
    - set_scale(${scale})

file_format: 1
