# auto-generated by grc.converter

id: sandia_utils_stream_gate
label: Stream Gate
category: '[Sandia]/Sandia Utilities'

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [c,f,i,s,b]
    option_labels: [Complex, Float, Int, Short, Byte]
    option_attributes:
        input: [complex, float, int, short, byte]
        output: [complex, float, int, short, byte]
    hide: part
-   id: vlen
    label: Vec Length
    dtype: int
    default: 1
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: flow_data
    label: Flow Data?
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    hide: part
-   id: consume_data
    label: Consume Data?
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    hide: part

inputs:
-   domain: stream
    dtype: ${ type.output }
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: ${ type.input }
    vlen: ${ vlen }

asserts:
- ${ vlen > 0 }

templates:
    imports: from gnuradio import sandia_utils
    make: sandia_utils.stream_gate_${type}(${flow_data},${consume_data}, ${vlen})
    callbacks:
    - set_flow_data(${flow_data})
    - set_consume_data(${consume_data})

file_format: 1
