# auto-generated by grc.converter

id: sandia_utils_file_monitor
label: File Monitor
category: '[Sandia]/Sandia Utilities/File Operators'

parameters:
-   id: nfiles
    label: Number of Files
    dtype: int
    default: '60'
-   id: output_path
    label: Output Path
    dtype: string
-   id: copy_data
    label: Copy Data?
    dtype: int
    default: 'False'
    options: ['1', '0']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: pdu
    optional: true

templates:
    imports: from gnuradio import sandia_utils
    make: sandia_utils.file_monitor(${nfiles},${output_path},${copy_data})
    callbacks:
    - set_copy(${copy_data})
    - set_output_path(${output_path})
    - set_nfiles(${nfiles})

file_format: 1
