# auto-generated by grc.converter

id: sandia_utils_vita49_tcp_msg_source
label: VITA49 TCP msg source
category: '[Sandia]/Sandia Utilities'

parameters:
-   id: port
    label: TCP Port
    dtype: int
    default: int(8207)
-   id: ignoretime
    label: Ignore Time?
    dtype: bool
    default: 'False'
    options: ['True', 'False']
-   id: ignoretune
    label: Ignore Tune?
    dtype: bool
    default: 'False'
    options: ['True', 'False']

outputs:
-   domain: message
    id: out
    optional: true
-   domain: message
    id: tune
    optional: true

templates:
    imports: import sandia_utils
    make: |-
       sandia_utils.vita49_tcp_msg_source(${port})
       self.${id}.setIgnoreTime(${ignoretime})
       self.${id}.setIgnoreTune(${ignoretune})
    
    callbacks:
    - setIgnoreTime(${ignoretime})
    - setIgnoreTune(${ignoretune})

file_format: 1
