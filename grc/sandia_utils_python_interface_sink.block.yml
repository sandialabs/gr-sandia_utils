# auto-generated by grc.converter

id: sandia_utils_python_interface_sink
label: Python Interface Sink
category: '[Sandia]/Sandia Utilities'

parameters:
-   id: maxsize
    label: Queue Size
    dtype: int
    default: '100'
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, short, byte]
    hide: part
-   id: vec_len
    label: Vector Length
    dtype: int
    default: '1'
    hide: ${ ('part' if int(vlen) == 1 else 'none') }

inputs:
-   domain: stream
    dtype: ${ type.t }
    vlen: ${ vec_len }
    optional: true

templates:
    imports: import sandia_utils
    make: sandia_utils.python_interface_sink(${maxsize}, ${vec_len})

documentation: |-
    Data bridge between the flowgraph and the controlling python script.

    This block uses an internal python Queue to hold incoming data. In order to empty the queue, call the get_data() function from the python flowgraph script. Note that if the internal queue fills up, it may halt the rest of the flowgraph.

    Inputs:
        in: any input stream

    Internal Functions:
        get_data(timeout):
            get the next data sample from the queue (optionally specify timeout in seconds, otherwise it will block)
        flush_data():
            empty the queue

file_format: 1
