# auto-generated by grc.converter

id: sandia_utils_uhd_timed_cordic_emulator
label: UHD Timed CORDIC Emulator
category: '[Sandia]/Sandia Utilities/Debug'

parameters:
-   id: short
    label: File Type
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: [Complex, Short]
-   id: filename
    label: Filename
    dtype: file_open
-   id: loop
    label: Loop File?
    dtype: enum
    default: 'True'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
    hide: part
-   id: decimation
    label: Decimation
    dtype: int
-   id: samp_rate
    label: Sample Rate
    dtype: float
-   id: f0
    label: Initial Freq
    dtype: float
    hide: part
-   id: epoch
    label: Start Time
    dtype: float
    default: '0'
    hide: part

inputs:
-   domain: message
    id: command
    optional: true

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import sandia_utils
    make: sandia_utils.uhd_timed_cordic_emulator(${filename}, ${loop}, ${decimation},
        ${samp_rate}, ${f0}, ${short}, ${epoch})

documentation: |-
    This block allows for a broadband file to be decimated and retuned at specific times for downstream processing.

file_format: 1
