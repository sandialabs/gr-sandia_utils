# auto-generated by grc.converter

id: sandia_utils_max_every_n
label: Max Every N
category: '[Sandia]/Sandia Utilities/Debug'

parameters:
-   id: n
    label: N
    dtype: int

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import sandia_utils
    make: sandia_utils.max_every_n(${n})

documentation: |-
    Keeps the maximum input value every n inputs

file_format: 1
